import { SoulModule } from './module';

/**
 * 列模块
 */
export declare class Column extends SoulModule {
  /**
   * 表格的尺寸
   * @default 'default'
   * @type string
   */
  size: 'medium' | 'small' | 'mini';
}
