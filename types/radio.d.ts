import { SoulModule } from './module';

/**
 * 单选
 */
export declare class Radio extends SoulModule {
  /**
   * 表格的尺寸
   * @default 'default'
   * @type string
   */
  size: 'medium' | 'small' | 'mini';
}
