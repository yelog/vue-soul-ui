import { SoulModule } from './module';

/**
 * 快捷菜单模块
 */
export declare class Menu extends SoulModule {
  /**
   * 表格的尺寸
   * @default 'default'
   * @type string
   */
  size: 'medium' | 'small' | 'mini';
}
