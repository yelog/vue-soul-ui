import { SoulModule } from './module';

/**
 * 核心模块
 */
export declare class Table extends SoulModule {
  /**
   * 表格的尺寸
   * @default 'default'
   * @type string
   */
  size: 'medium' | 'small' | 'mini';
}
